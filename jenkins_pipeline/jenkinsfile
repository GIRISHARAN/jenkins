pipeline {
    agent {
        docker { image 'openjdk:11-jdk-slim' }
    }
    stages {
        stage('Check Java Version') {
            steps {
                sh 'java -version'
            }
        }
    }
}